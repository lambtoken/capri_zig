// bubble sort

arr = [4, 3, 2, 9 ,24, 1]

mut temp = nothing

for i in @len(arr)..0 {
    for j in @len(arr)..0 {
        if arr[j] < arr[i] {
            temp = arr[j]
            arr[j] = arr[i]
            arr[i] = temp
        }
    }
}

for i in arr {
    @print(i)
}
