@println("Starting 10,000 iteration test...")
for i in 1..10000 {
    x = i * 2
}

@println("10K test completed")

@println("Starting 100,000 iteration test...")
for i in 1..100000 {
    // Abusing the local env a bit
    x = i * 2 + 1
    y = x / 2
}

@println("100K test completed") 